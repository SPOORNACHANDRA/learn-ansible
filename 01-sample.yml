- name: install nginx
  hosts: WEB
  become: true #key word for privilage escalations use sudo in terminal
  tasks:
    - name: install nginx
      ansible.builtin.yum:
        name: httpd
        state: latest


    - name: start nginx
      ansible.builtin.systemd:
        name: nginx
        state: started
        enabled: yes